apiVersion: v1
kind: Service
metadata:
  name: {{ include "chc-web.fullname" . }}
  labels:
    {{ include "chc-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{ include "chc-web.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
